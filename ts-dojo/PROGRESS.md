# Прогресс TypeScript Dojo

TypeScript 5.9.3 · vitest 5.0.1 · Node 22
Флаги: `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`

## Текущий урок: 01 — Structural typing, assignability, excess property checks

Дожо теперь веб-приложение: https://ts-dojo-beta.vercel.app
Компилятор 5.9.3 работает в браузере, консольная версия в `lessons/` осталась.

Предсказание пройдено: **2 из 5** (верно (2) и (3), неверно (1), (4), (5)).
Упражнения не решены.

### Что показало предсказание

- Ошибки в (1) и (4) — одна и та же причина: объект пришёл **через переменную**, свежесть
  (freshness) потеряна, excess property check не срабатывает. Ожидал ошибку — её нет.
- В (2) и (3) ответ верный, но обоснование было про assignability («нет свойства z»), тогда как
  срабатывает вторая, отдельная проверка. Правильный ответ по неправильной причине.
- (5) spread: свойства из `...obj` свежести не несут, при этом **остаются в типе**
  (`{ retries: number; timeout: number }`). Собственный ключ литерала рядом со spread — свежий,
  и ошибку даёт.

### Главное расхождение в модели

Assignability и excess property check воспринимались как одна проверка. Это две разные:
первая структурная и лишнее ей не мешает никогда; вторая — эвристика поверх неё, срабатывает
только на свежем литерале.

## Пройдено

| # | Тема | Дата | Уверенность (1–5) | Слабые места |
|---|------|------|-------------------|--------------|
| 01 | Structural typing, assignability, excess property checks | 2026-09-20 | — (в работе) | freshness; смешение двух проверок |

## Повторить

- Freshness — свойство выражения, а не типа. Где теряется: переменная, spread, `as`.
- Почему excess property check не защищает на границах данных (API, конфиг) и что там нужно вместо неё.

## Вопросы для собеседования, на которых я споткнулся

- «Почему `draw(cube)` проходит, а `draw({ x, y, z })` — нет, если объект один и тот же?»
