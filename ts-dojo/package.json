{
  "name": "ts-dojo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run --typecheck",
    "test:watch": "vitest --typecheck"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^5.0.1"
  }
}
